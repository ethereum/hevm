# `hevm abiencode`

```
Usage: hevm abiencode [--abi STRING] [--arg STRING]...

Available options:
  -h,--help                Show this help text
  --abi STRING             Signature of types to decode / encode
  --arg STRING             Values to encode
```
